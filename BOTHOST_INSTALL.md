# 🚀 УСТАНОВКА БОТА НА BOTHOST.RU

## 📦 Файлы для загрузки:

1. **bot.py** - основной код бота
2. **requirements.txt** - зависимости Python

---

## ⚙️ ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Регистрация и создание бота

1. Зайдите на https://bothost.ru/dashboard.php
2. Нажмите **"Создать нового бота"**
3. Выберите тариф (рекомендуется **"Стандарт"** или выше для больших файлов)

---

### Шаг 2: Загрузка файлов

1. В панели управления ботом найдите **"Файлы"**
2. Загрузите **bot.py** (основной файл)
3. Загрузите **requirements.txt** (зависимости)

**Структура файлов должна быть:**
```
/
├── bot.py
└── requirements.txt
```

---

### Шаг 3: Настройка переменных окружения

1. Перейдите в раздел **"Настройки"** → **"Переменные окружения"**
2. Добавьте **ДВЕ** переменные:

#### **Переменная 1: TELEGRAM_TOKEN**
```
Название: TELEGRAM_TOKEN
Значение: ваш_токен_от_BotFather
```

**Как получить токен Telegram:**
- Откройте Telegram
- Найдите бота **@BotFather**
- Отправьте команду `/newbot` (если создаёте нового)
- Или `/mybots` → выберите бота → `API Token`
- Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### **Переменная 2: DYXLESS_API_TOKEN**
```
Название: DYXLESS_API_TOKEN
Значение: ваш_токен_dyxless
```

**Как получить токен Dyxless:**
- Откройте Telegram
- Найдите бота **@dyxless_bot**
- Получите API токен через команды бота
- Скопируйте токен

---

### Шаг 4: Установка зависимостей

1. Перейдите в раздел **"Консоль"** или **"SSH"**
2. Выполните команду:

```bash
pip install -r requirements.txt
```

Или по отдельности:
```bash
pip install aiogram==2.25.1
pip install aiohttp==3.9.1
pip install pandas==2.1.4
pip install openpyxl==3.1.2
```

---

### Шаг 5: Создание директорий

В консоли выполните:

```bash
mkdir -p temp
mkdir -p checkpoints
```

Эти директории нужны для:
- `temp` - временные файлы
- `checkpoints` - автосохранение прогресса

---

### Шаг 6: Настройка команды запуска

1. Перейдите в **"Настройки"** → **"Команда запуска"**
2. Введите:

```bash
python3 bot.py
```

---

### Шаг 7: Запуск бота

1. Нажмите **"Запустить бота"**
2. Проверьте логи (раздел **"Логи"**)
3. Должно появиться:

```
🤖 Dyxless Bot Enterprise запущен...
✨ Поддержка больших файлов с автосохранением
```

---

### Шаг 8: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должно появиться меню:

```
👋 Добро пожаловать в Dyxless Search Bot!

📊 Поиск по файлу с ИНН - до 10 000+ контактов
🔍 Одиночный поиск - быстрый поиск
📈 Статус лимита API - мониторинг
```

---

## ✅ ПРОВЕРКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

Чтобы убедиться, что переменные установлены правильно:

1. Зайдите в консоль BotHost
2. Выполните:

```bash
echo $TELEGRAM_TOKEN
echo $DYXLESS_API_TOKEN
```

Должны отобразиться ваши токены.

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### ❌ Ошибка: "Не установлены переменные окружения"

**Решение:**
- Проверьте правильность названий переменных:
  - `TELEGRAM_TOKEN` (не `TELEGRAM_BOT_TOKEN`)
  - `DYXLESS_API_TOKEN` (не `DYXLESS_TOKEN`)
- Перезапустите бота после добавления переменных

---

### ❌ Ошибка: "ModuleNotFoundError: No module named 'aiogram'"

**Решение:**
```bash
pip install -r requirements.txt
```

---

### ❌ Ошибка: "Permission denied"

**Решение:**
```bash
chmod +x bot.py
```

---

### ❌ Бот не отвечает в Telegram

**Проверьте:**
1. **Токен Telegram** правильный:
   - Проверьте через @BotFather
   - Нет лишних пробелов в переменной

2. **Бот запущен:**
   - Смотрите логи в панели BotHost
   - Должно быть "Dyxless Bot Enterprise запущен"

3. **Нет конфликтов:**
   - Остановите все другие экземпляры бота
   - Один токен = один запущенный бот

---

## 📊 НАСТРОЙКА ЛИМИТОВ (ОПЦИОНАЛЬНО)

По умолчанию:
- Лимит API: **100 запросов за 15 минут**
- Пауза: **16 минут** при превышении
- Автосохранение: **каждые 50 запросов**
- Отправка файлов: **каждые 100 запросов**

Если хотите изменить, отредактируйте в `bot.py`:

```python
# Строка ~49
rate_limiter = SimpleRateLimiter(max_calls=100, wait_minutes=16)
```

Измените на:
```python
rate_limiter = SimpleRateLimiter(max_calls=95, wait_minutes=15)
```

---

## 🎯 РЕКОМЕНДАЦИИ ПО ТАРИФУ

### Для разных объёмов:

| Объём файлов | Тариф BotHost | RAM | Причина |
|--------------|---------------|-----|---------|
| До 500 строк | **Начальный** | 256 MB | Достаточно |
| 500-2000 строк | **Стандарт** | 512 MB | Рекомендуется |
| 2000-5000 строк | **Стандарт** | 512 MB | Минимум |
| 5000-10000 строк | **Премиум** | 1 GB | Обязательно |
| 10000+ строк | **Премиум** | 1 GB+ | Обязательно |

**Почему важна RAM:**
- Обработка Excel файлов требует памяти
- 10 000 строк ≈ 200-300 MB RAM
- + память для pandas, openpyxl
- + буфер для стабильности

---

## 🔄 ОБНОВЛЕНИЕ БОТА

Если нужно обновить код:

1. Остановите бота в панели BotHost
2. Замените файл `bot.py` новой версией
3. Запустите бота снова

**Переменные окружения сохраняются!**

---

## 💾 РЕЗЕРВНОЕ КОПИРОВАНИЕ

BotHost автоматически сохраняет:
- ✅ Код бота
- ✅ Переменные окружения
- ✅ Файловую структуру

**Но чекпоинты удаляются при перезапуске!**

Чтобы сохранить прогресс при обновлении:
1. Дождитесь завершения текущей обработки
2. Или скачайте папку `checkpoints` перед обновлением

---

## 📱 МОНИТОРИНГ РАБОТЫ

### В панели BotHost смотрите:

1. **Логи** - все события бота
2. **Использование RAM** - не должно превышать лимит
3. **CPU** - нагрузка при обработке
4. **Сеть** - трафик API запросов

### Нормальные значения при обработке:
- RAM: 200-400 MB (для 1000 строк)
- CPU: 10-30%
- Сеть: стабильная активность

---

## 🚨 ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Не храните токены в коде!
❌ **НЕПРАВИЛЬНО:**
```python
TELEGRAM_TOKEN = "1234567890:ABCdef..."
```

✅ **ПРАВИЛЬНО:**
```python
TELEGRAM_TOKEN = os.getenv('TELEGRAM_TOKEN')
```

### 2. Директории temp и checkpoints
- Создаются автоматически при первом запуске
- Но лучше создать вручную заранее

### 3. Лимит Telegram
- Файлы до **50 MB** можно отправить
- Для больших файлов (>50 MB) - будет ошибка
- 10 000 строк ≈ 15-20 MB (обычно ОК)

### 4. Долгая обработка
- BotHost может перезапустить бот через 24 часа
- Для 10 000 контактов (~26 часов) нужен тариф без ограничений
- Или разбейте файл на части по 5000 строк

---

## 📋 ЧЕКЛИСТ УСТАНОВКИ

- [ ] Зарегистрирован на BotHost.ru
- [ ] Создан бот в панели
- [ ] Загружен файл `bot.py`
- [ ] Загружен файл `requirements.txt`
- [ ] Добавлена переменная `TELEGRAM_TOKEN`
- [ ] Добавлена переменная `DYXLESS_API_TOKEN`
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Созданы директории `temp` и `checkpoints`
- [ ] Установлена команда запуска `python3 bot.py`
- [ ] Бот запущен
- [ ] Проверена работа через `/start` в Telegram
- [ ] Протестирован одиночный поиск
- [ ] Протестирована загрузка файла (маленького)

---

## 🎯 ИТОГОВАЯ КОМАНДА УСТАНОВКИ

Для быстрой установки выполните в консоли BotHost:

```bash
# Установка зависимостей
pip install -r requirements.txt

# Создание директорий
mkdir -p temp checkpoints

# Проверка переменных
echo "TELEGRAM_TOKEN: $TELEGRAM_TOKEN"
echo "DYXLESS_API_TOKEN: $DYXLESS_API_TOKEN"

# Запуск
python3 bot.py
```

---

## 📞 ПОДДЕРЖКА

### Проблемы с BotHost:
- Документация: https://bothost.ru/docs
- Поддержка: через панель BotHost

### Проблемы с ботом:
- Проверьте логи в панели BotHost
- Убедитесь, что переменные окружения установлены
- Проверьте токены в @BotFather и @dyxless_bot

---

**Готово! Теперь у вас есть всё для установки на BotHost!** 🚀
